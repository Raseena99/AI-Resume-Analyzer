# AI-Resume-Analyzer
AI-powered resume analysis system built using FastAPI and NLP techniques to automate candidate evaluation. The application extracts resume content from PDF files, performs skill matching against job requirements, calculates resume scores using cosine similarity, and exposes REST APIs for scalable backend processing.
